version: '3.8'
services:
  qdrant:
    image: qdrant/qdrant
    ports:
      - "6333:6333" # Expose Qdrant on port 6333
    volumes:
      - qdrant_data:/qdrant/data # Persist Qdrant data
    environment:
      QDRANT__STORAGE__STORAGE_PATH: "/qdrant/data"

volumes:
  qdrant_data:
